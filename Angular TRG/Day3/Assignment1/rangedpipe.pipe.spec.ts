import { RangedpipePipe } from './rangedpipe.pipe';

describe('RangedpipePipe', () => {
  it('create an instance', () => {
    const pipe = new RangedpipePipe();
    expect(pipe).toBeTruthy();
  });
});
